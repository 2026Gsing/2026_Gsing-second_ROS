# Python 依赖
# ROS2 包来自系统 apt（不需要 pip）：
#   sudo apt install ros-jazzy-nav2-bringup ros-jazzy-nav2-msgs ros-jazzy-sensor-msgs-py

# YOLO 视觉
ultralytics>=8.3.0
opencv-python>=4.8.0
numpy>=1.24.0
pillow>=10.0.0

# LiDAR 点云聚类（cube_detector.py: DBSCAN + BallTree）
scikit-learn>=1.3.0

# 串口通信（listen_serial.py, test_interactive.py）
pyserial>=3.5

# 配置文件（auto_task.py）
pyyaml>=6.0

# 3D 点云处理（fast_lio_localization 使用，非必需）
# open3d>=0.17.0
# tf-transformations>=1.0.2

# 可视化（可选）
# matplotlib>=3.7.0
