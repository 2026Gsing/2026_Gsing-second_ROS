# Python 依赖
# ROS2 包来自系统 apt（不需要 pip）：
#   sudo apt install ros-jazzy-nav2-bringup ros-jazzy-nav2-msgs

# YOLO 视觉
ultralytics>=8.3.0
opencv-python>=4.8.0
numpy>=1.24.0
pillow>=10.0.0

# LiDAR 点云聚类（cube_detector.py: DBSCAN + BallTree）
scikit-learn>=1.3.0

# 串口通信（cmd_vel_chassis_serial.py, listen_serial.py）
pyserial>=3.5

# 配置文件（auto_task.py, competition.yaml）
pyyaml>=6.0

# 3D 点云处理（fast_lio_localization ICP 配准使用）
open3d>=0.17.0
tf-transformations>=1.0.2

# ROS2 传感器消息 Python 绑定
# sudo apt install ros-jazzy-sensor-msgs-py
