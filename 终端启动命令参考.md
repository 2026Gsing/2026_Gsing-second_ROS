# 终端启动命令参考

> 所有终端均需 `export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp`
> 工作空间：`/home/hyper/program/2026_Gsing-second_ROS`

---

## 串口桥

### 启动串口桥（连接 STM32，所有机械臂/底盘控制的前提）

```bash
export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
cd /home/hyper/program/2026_Gsing-second_ROS/nav2_ws1
source /opt/ros/humble/setup.bash && source install/setup.bash
ros2 launch dog_nav2_bringup chassis_serial_bridge.launch.py \
  serial_port:=/dev/ttyACM0 baud_rate:=115200 \
  cmd_vel_topic:=/cmd_vel send_rate_hz:=50.0 \
  active_state:=1 idle_state:=0
```

### 串口权限（如果打不开）

```bash
sudo chmod 666 /dev/ttyACM0
```

---

## 机械臂控制

### 依赖关系

```
串口桥 (必须最先启动)
  ├── catch.py             ← 视觉抓取（依赖 cube_detector 提供 /detected_cube）
  ├── box_pick_node.py     ← 独立抓取节点（内嵌 cube_detector + catch）
  └── 手动话题测试          ← 手动发坐标测试
```

### LiDAR 立方体检测（catch.py 的前置条件）

```bash
export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
source /opt/ros/humble/setup.bash
cd /home/hyper/program/2026_Gsing-second_ROS
python3 py/cube_detector.py
```

### 视觉抓取 (catch.py)

```bash
export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
source /opt/ros/humble/setup.bash
cd /home/hyper/program/2026_Gsing-second_ROS
python3 py/catch.py
```

### 独立抓取节点（整合 cube_detector + catch）

```bash
export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
source /opt/ros/humble/setup.bash
cd /home/hyper/program/2026_Gsing-second_ROS
python3 py/box_pick_node.py
```

### 手动发坐标测试机械臂（串口桥运行中可用）

```bash
# 回到 HOME 位置
ros2 topic pub /vision/arm_control std_msgs/String \
  'data: "{\"x\": 0.22, \"y\": 0.0, \"z\": -0.08}"' --once

# 移动到前方抓取位置
ros2 topic pub /vision/arm_control std_msgs/String \
  'data: "{\"x\": 0.3, \"y\": 0.1, \"z\": 0.0}"' --once

# 发送完整 ARM_MISSION（多段 pick→back→place 任务）
ros2 topic pub /vision/arm_mission std_msgs/String \
  'data: "{\"mode\": 1, \"flags\": 7, \"pick\": [0.25, 0.1, 0.0], \"back\": [0.3, 0.36, 0.3], \"place\": [0.25, 0.0, 0.363]}"' --once
```

---

## 底盘控制

### 单独测试底盘（串口桥运行中可用）

```bash
# 测试前进
ros2 topic pub /cmd_vel geometry_msgs/Twist \
  '{linear: {x: 0.3}, angular: {z: 0.0}}'

# 测试转弯
ros2 topic pub /cmd_vel geometry_msgs/Twist \
  '{linear: {x: 0.0}, angular: {z: 0.3}}'

# 停止
ros2 topic pub /cmd_vel geometry_msgs/Twist \
  '{linear: {x: 0.0}, angular: {z: 0.0}}'
```

### test_move.py（交互式底盘+机械臂综合测试）

```bash
export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
source /opt/ros/humble/setup.bash
cd /home/hyper/program/2026_Gsing-second_ROS
python3 py/test_move.py
```

---

## 自动任务事件

> 以下由 `py/control/auto_task.py` 自动发送，也可手动测试。

### 视觉自动任务状态机

```bash
export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
source /opt/ros/humble/setup.bash
cd /home/hyper/program/2026_Gsing-second_ROS
python3 py/control/auto_task.py
```

### 手动发送自动任务事件

```bash
# 启动
ros2 topic pub /vision/auto_cmd std_msgs/String \
  'data: "{\"cmd\": 1, \"target\": 0, \"zone\": 0}"' --once

# 已到达物资箱
ros2 topic pub /vision/auto_cmd std_msgs/String \
  'data: "{\"cmd\": 2, \"target\": 1, \"zone\": 0}"' --once

# 已到达归位区
ros2 topic pub /vision/auto_cmd std_msgs/String \
  'data: "{\"cmd\": 4, \"target\": 0, \"zone\": 1}"' --once

# 抓取完成
ros2 topic pub /vision/auto_cmd std_msgs/String \
  'data: "{\"cmd\": 5, \"target\": 0, \"zone\": 0}"' --once

# 紧急停止
ros2 topic pub /vision/auto_cmd std_msgs/String \
  'data: "{\"cmd\": 8, \"target\": 0, \"zone\": 0}"' --once
```

---

## 调试工具

### 串口监听

```bash
export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
source /opt/ros/humble/setup.bash
cd /home/hyper/program/2026_Gsing-second_ROS
python3 py/listen_serial.py
```

### 到达检测（单独测试）

```bash
export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
source /opt/ros/humble/setup.bash
cd /home/hyper/program/2026_Gsing-second_ROS
python3 py/arrival_detector.py
```

---

## YOLO 视觉检测

```bash
cd /home/hyper/program/2026_Gsing-second_ROS/vision
python3 src/predict.py --weights weights/task3.pt --source 1 --draw-roi
```
